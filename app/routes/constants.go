package routes

const routeNameHeartbeat = "heartbeat"
const routeNameSayError = "say-error"
const routeNameAddContact = "contact"

const routeNameAppHeartbeat = "app-heartbeat"
const routeNameAppSayError = "app-say-error"

const pathHeartbeat = "/"
const pathSayError = "/error"
const pathLookup = "/postal-codes/{code}"

const pathPrefixV1 = "/v1"
const pathPrefixV2 = "/v2"

const appName = "ms-contact-manager"

const pathContact = "/contact"
